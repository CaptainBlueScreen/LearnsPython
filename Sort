def bubbleSort(arr):
  length = len(arr)
  for i in range(length):
    for j in range(1, length - i):
      if arr[j] < arr[j-1]:
        swap(arr, j, j-1)

  return arr

def swap(arr, i, j):
  temp = arr[i]
  arr[i] = arr[j]
  arr[j] = temp
